namespace Pt.Okx.Sdk.Indicators.Enums
{
    /// <summary>
    /// Chart window where an indicator should be displayed.
    /// </summary>
    public enum IndicatorWindow
    {
        /// <summary>Render the indicator on the main price chart.</summary>
        Main,

        /// <summary>Render the indicator in a separate pane.</summary>
        Separate,
    }
}
